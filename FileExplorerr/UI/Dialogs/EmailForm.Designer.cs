// EmailForm.Designer.cs
//
// La UI de EmailForm se construye íntegramente en código (EmailForm.cs → BuildUI()).
// Este archivo Designer se mantiene vacío para no romper referencias de MSBuild,
// pero no contiene código generado porque no se usa el diseñador visual.

namespace FileExplorerr
{
    partial class EmailForm
    {
        private System.ComponentModel.IContainer? components = null;

        protected override void Dispose(bool disposing)
        {
            if (disposing) components?.Dispose();
            base.Dispose(disposing);
        }
    }
}